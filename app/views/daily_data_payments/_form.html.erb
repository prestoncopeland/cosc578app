<%= form_for(@daily_data_payment) do |f| %>
  <% if @daily_data_payment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@daily_data_payment.errors.count, "error") %> prohibited this daily_data_payment from being saved:</h2>

      <ul>
      <% @daily_data_payment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="container">
    <%= f.label :date %><br>
    <%= f.date_select :date %>
  </div>
  <div class="container">
    <%= f.label :student_id, 'Student'%><br>
    <%= f.select :student_id, options_for_select(@students) %>
  </div>
  <div class="container">
    <%= f.label :amount %><br>
    <%= f.text_field :amount %>
  </div>
  <div class="container">
    <%= f.label :payment_type %><br>
    <%= f.select :payment_type, options_for_select(@payment_types) %>
  </div>
  <div class="container">
    <%= f.label :partial_payment, 'Partial payment(check for yes)' %><br>
    <%= f.check_box :partial_payment %>
  </div>

  <div class="container">
    <%= f.label :referral_source %><br>
    <%= f.select :referral_source, options_for_select(@referral_sources) %>
  </div>
  <div class="container">
    <%= f.label :student_action %><br>
    <%= f.select :student_action, options_for_select(@student_actions) %>
  </div>

  <div class="container">
    <%= f.label :notes %><br>
    <%= f.text_area :notes %>
  </div>
  <div class="container">
    <%= f.label :rate %><br>
    <%= f.text_field :rate %>
  </div>
  <div class="container">
    <%= f.label :hours %><br>
    <%= f.text_field :hours %>
  </div>
  <div class="container">
    <%= f.submit class: "btn btn-primary"%>
  </div>
<% end %>
</br></br>


