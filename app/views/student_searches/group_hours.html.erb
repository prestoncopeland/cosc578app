<h1 class = "container">Hours Used For Multiple Students</h1>
<div class="row">
        <div class="col-xs-12 col-sm-6 col-md-4 col-lg-4">

<p>
  <table class = "container">
  <thead>
    <tr>
      <th>Student </th>
      <th>Hours Used</th>
    </tr>
  </thead>

  <tbody>
    <% @students.each do |student| %>
      <% if student != nil %>
      <tr>
        <td><%= student.nickname %> </td>
        <td><%= student.hours_used %></td>
      </tr>
    <% end %>
    <% end %>
  </tbody>
</table>
    </p>

<p><%= link_to 'More Student Queries', student_searches_path, class: "btn btn-primary" %>
        </p>

<%= render 'shared/student_pic' %>