<div class = "container">
<h1>Listing Siblings of Student</h1>

<table>
  <thead>
    <tr>
      <th>First name</th>
      <th>Last name</th>
      <th>Grade</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @siblings.each do |sibling| %>
      <tr>
        <td><%= sibling.first_name %></td>
        <td><%= sibling.last_name %></td>
        <td><%= sibling.grade %></td>
        <td><%= link_to 'Show', sibling %></td>
        <td><%= link_to 'Edit', edit_sibling_path(sibling) %></td>
        <td><%= link_to 'Destroy', sibling, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br><br>

<%= link_to 'New Sibling', new_sibling_path, class: "btn btn-primary" %>

<br><br>

<%= link_to 'More Relative Queries', student_searches_student_relatives_path, class: "btn btn-primary" %>
</div>